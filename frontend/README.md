# Frontend

All browser-delivered assets live in `public/`. The backend serves this directory at the site root, so existing URLs such as `/index.html`, `/guide-admin.html`, and `/styles.css` remain unchanged.

The frontend uses plain HTML, CSS, and JavaScript with same-origin API requests to `/api/*`.
