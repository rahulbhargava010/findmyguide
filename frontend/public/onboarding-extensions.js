(()=>{
  const step3=document.querySelector('[data-step="3"]'),step4=document.querySelector('[data-step="4"]');
  if(step3){
    const states=['Andaman and Nicobar Islands','Andhra Pradesh','Arunachal Pradesh','Assam','Bihar','Chandigarh','Chhattisgarh','Dadra and Nagar Haveli and Daman and Diu','Delhi','Goa','Gujarat','Haryana','Himachal Pradesh','Jammu and Kashmir','Jharkhand','Karnataka','Kerala','Ladakh','Lakshadweep','Madhya Pradesh','Maharashtra','Manipur','Meghalaya','Mizoram','Nagaland','Odisha','Puducherry','Punjab','Rajasthan','Sikkim','Tamil Nadu','Telangana','Tripura','Uttar Pradesh','Uttarakhand','West Bengal'],types=[['city','City / town'],['attraction','Tourist attraction'],['monument','Monument / fort'],['palace','Palace'],['museum','Museum'],['reserve','Wildlife reserve'],['park','National park'],['district','District'],['region','Circuit / region']];
    for(const field of [step3.querySelector('[name="states"]'),step3.querySelector('[name="workLocations"]')])if(field){field.required=false;field.closest('label').hidden=true}
    const block=document.createElement('div');block.className='service-area-fields';block.innerHTML='<div class="coverage-heading"><div><h3>Where can travelers book you?</h3><p class="field-help">Add each city, palace, monument, park or other place separately. These entries power search and your public profile.</p></div><button class="button" type="button" id="addCoverage">+ Add place</button></div><div id="coverageRows"></div><textarea name="serviceCities" hidden></textarea><textarea name="serviceAttractions" hidden></textarea>';step3.querySelector('.step-actions').before(block);
    const rows=block.querySelector('#coverageRows'),options=types.map(([value,label])=>`<option value="${value}">${label}</option>`).join(''),stateOptions=states.map(state=>`<option>${state}</option>`).join('');
    function sync(){const areas=[...rows.querySelectorAll('[data-service-row]')].map(row=>({type:row.querySelector('[data-area-type]').value,name:row.querySelector('[data-area-name]').value.trim(),city:row.querySelector('[data-area-city]').value.trim(),state:row.querySelector('[data-area-state]').value}));block.querySelector('[name="serviceCities"]').value=areas.filter(area=>area.type==='city'&&area.name).map(area=>`${area.name} | ${area.state}`).join('\n');block.querySelector('[name="serviceAttractions"]').value=areas.filter(area=>area.type!=='city'&&area.name).map(area=>`${area.name} | ${area.city||area.name} | ${area.state} | ${area.type}`).join('\n')}
    function addRow(data={}){const row=document.createElement('div');row.className='coverage-row';row.dataset.serviceRow='';row.innerHTML=`<label>Area type<select data-area-type>${options}</select></label><label>Place or city name <b>*</b><input data-area-name required placeholder="e.g. City Palace"></label><label>Nearest city<input data-area-city placeholder="e.g. Jaipur"></label><label>State / UT <b>*</b><select data-area-state required><option value="">Select state / UT</option>${stateOptions}</select></label><button type="button" data-remove-area aria-label="Remove coverage">×</button>`;row.querySelector('[data-area-type]').value=data.type||'city';row.querySelector('[data-area-name]').value=data.name||'';row.querySelector('[data-area-city]').value=data.city||'';row.querySelector('[data-area-state]').value=data.state||'';row.querySelector('[data-remove-area]').onclick=()=>{if(rows.children.length>1)row.remove();sync()};row.addEventListener('input',sync);row.addEventListener('change',sync);rows.appendChild(row);sync()}
    block.querySelector('#addCoverage').onclick=()=>addRow({type:'attraction'});addRow();step3.closest('form').addEventListener('submit',sync,{capture:true});
  }
  if(step4){const block=document.createElement('div');block.className='government-documents';block.innerHTML='<h3>Start with one identity document</h3><p class="field-help">A guide licence is not compulsory. Submit one photo ID for identity verification; professional credentials can be added now or later. For Aadhaar, use a masked copy or UIDAI offline QR/XML—we retain only the last four digits.</p><div class="two-col"><label>Identity document<select name="governmentIdKind" required><option value="aadhaar">Masked Aadhaar / UIDAI offline verification</option><option value="voter_id">Voter ID</option><option value="driving_licence">Driving licence</option><option value="passport">Passport</option><option value="pan">PAN card</option><option value="other_government_photo_id">Other government photo ID</option></select></label><label>Last 4 digits / document reference <b>*</b><input name="governmentIdNumber" required autocomplete="off" maxlength="30" placeholder="Last 4 digits only for Aadhaar"></label><label>Issuing authority <b>*</b><input name="governmentIdAuthority" required placeholder="UIDAI, Election Commission, Passport Office…"></label><label>Upload masked ID or offline proof <b>*</b><input name="governmentIdFile" type="file" accept="image/jpeg,image/png,application/pdf,.xml,.zip" required></label></div><h3>Professional credentials <small>(optional)</small></h3><div class="two-col"><label>Credential type<select name="guideCredentialType"><option value="">Add later</option><option value="tourism_guide_license">State/UT tourism guide licence</option><option value="incredible_india_certificate">Incredible India IITF/IITG certificate</option><option value="forest_department_id">Forest Department ID</option><option value="archaeology_museum_id">Archaeology / Museums / ASI credential</option><option value="first_aid_certificate">First-aid certificate</option><option value="other_government_credential">Other government credential</option></select></label><label>Credential number<input name="guideCredentialNumber" placeholder="Licence / certificate number"></label><label>Issuing authority<input name="guideCredentialAuthority" placeholder="Central, state or UT authority"></label><label>Upload credential<input name="guideCredentialFile" type="file" accept="image/jpeg,image/png,application/pdf"></label></div>';step4.querySelector('.upload-grid').before(block);block.querySelectorAll('input[type="file"]').forEach(input=>input.addEventListener('change',()=>{const file=input.files[0];if(file&&file.size>1_500_000){input.value='';alert(`${file.name} is larger than 1.5 MB. Please upload a smaller or compressed copy.`)}}))}
})();
