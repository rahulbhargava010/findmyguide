(()=>{const esc=value=>String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char])),dialog=document.querySelector('.record-dialog'),content=document.querySelector('#recordDialogContent'),validImage=value=>String(value||'').startsWith('data:image/')||String(value||'').startsWith('http');
function gallery(profile,work=[]){const images=[profile,...work].filter(validImage);return `<section class="admin-record-gallery"><h3>Provided images</h3>${images.length?`<div>${images.map((image,index)=>`<a href="${image}" target="_blank" rel="noopener"><img src="${image}" alt="Guide provided image ${index+1}"></a>`).join('')}</div>`:'<p>No stored images. Older filename-only uploads must be submitted again.</p>'}</section>`}
function coverageLines(areas=[]){return areas.map(area=>`${area.type} | ${area.name} | ${area.city||''} | ${area.state||''}`).join('\n')}
function parseList(value){return String(value||'').split(',').map(item=>item.trim()).filter(Boolean)}function parseCoverage(value){return String(value||'').split('\n').map(line=>{const [type,name,city,state]=line.split('|').map(item=>item.trim());return{type,name,city,state,country:'India'}}).filter(area=>area.name&&area.state)}
async function openGuide(id){content.innerHTML='<p>Loading complete guide record…</p>';dialog.showModal();try{const {guide}=await adminApi(`/api/admin/guides/${id}`);content.innerHTML=`<span class="kicker">GUIDE RECORD</span><div class="record-title"><h2>${esc(guide.display_name)}</h2><span>${guide.government_verified?'✓ Identity verified':'Standard profile'}</span></div>${gallery(guide.profile_photo,guide.workPhotos)}<form id="adminGuideEdit" class="admin-edit-form"><div class="two-col"><label>Public name<input name="name" required value="${esc(guide.display_name)}"></label><label>Primary location<input name="primaryLocation" required value="${esc(guide.primary_location)}"></label><label>Years of experience<input name="yearsExperience" type="number" min="0" value="${guide.years_experience}"></label><label>Daily rate<input name="dailyRate" type="number" min="0" value="${guide.daily_rate}"></label><label>Expertise<input name="expertise" value="${esc(guide.expertise.join(', '))}"></label><label>Languages<input name="languages" value="${esc(guide.languages.join(', '))}"></label><label>Profile status<select name="verificationStatus"><option ${guide.verification_status==='approved'?'selected':''}>approved</option><option ${guide.verification_status==='review'?'selected':''}>review</option><option ${guide.verification_status==='suspended'?'selected':''}>suspended</option></select></label></div><label>Biography<textarea name="bio">${esc(guide.bio)}</textarea></label><label>Service coverage<textarea name="serviceAreas" placeholder="type | place | city | state">${esc(coverageLines(guide.serviceAreas))}</textarea><small>One per line: type | place | city | state/UT</small></label><div class="record-readonly"><span>Email<b>${esc(guide.email||'—')}</b></span><span>Phone<b>${esc(guide.phone||'—')}</b></span><span>Work locations<b>${esc(guide.workLocations.join(', '))}</b></span><span>Rating<b>${guide.rating} ★ · ${guide.review_count} reviews</b></span></div><button class="button button-dark" type="submit">Save guide changes</button></form>`;document.querySelector('#adminGuideEdit').onsubmit=async event=>{event.preventDefault();const form=new FormData(event.currentTarget);await adminApi(`/api/admin/guides/${id}`,{method:'PATCH',body:{name:form.get('name'),primaryLocation:form.get('primaryLocation'),yearsExperience:+form.get('yearsExperience'),dailyRate:+form.get('dailyRate'),expertise:parseList(form.get('expertise')),languages:parseList(form.get('languages')),verificationStatus:form.get('verificationStatus'),bio:form.get('bio'),serviceAreas:parseCoverage(form.get('serviceAreas'))}});await loadAdminDirectories();showToast('Guide profile updated');dialog.close()}}catch(error){content.innerHTML=`<p>${esc(error.message)}</p>`}}
async function openUser(id){content.innerHTML='<p>Loading complete user record…</p>';dialog.showModal();try{const data=await adminApi(`/api/admin/users/${id}`),user=data.user,guide=data.guide,traveler=data.traveler;content.innerHTML=`<span class="kicker">COMPLETE USER RECORD</span><div class="record-title"><h2>${esc(user.name)}</h2><span>${esc(user.role)} · ${esc(user.status)}</span></div>${guide?gallery(guide.profile_photo,guide.workPhotos):''}<form id="adminUserEdit" class="admin-edit-form"><div class="two-col"><label>Name<input name="name" required value="${esc(user.name)}"></label><label>Email<input name="email" type="email" required value="${esc(user.email)}"></label><label>Phone<input name="phone" value="${esc(user.phone||'')}"></label><label>Account status<select name="status"><option ${user.status==='active'?'selected':''}>active</option><option ${user.status==='suspended'?'selected':''}>suspended</option></select></label></div><div class="record-readonly"><span>Role<b>${esc(user.role)}</b></span><span>Joined<b>${new Date(user.created_at).toLocaleString()}</b></span>${traveler?`<span>Home location<b>${esc(traveler.home_location||'—')}</b></span><span>Interests<b>${esc(traveler.interests.join(', ')||'—')}</b></span>`:''}${guide?`<span>Guide name<b>${esc(guide.display_name)}</b></span><span>Coverage<b>${esc(guide.serviceAreas.map(area=>`${area.name}, ${area.state}`).join(' · ')||guide.workLocations.join(', '))}</b></span><span>Expertise<b>${esc(guide.expertise.join(', '))}</b></span><span>Biography<b>${esc(guide.bio)}</b></span>`:''}<span>Booking history<b>${data.bookings.length?data.bookings.map(booking=>`${booking.reference} · ${booking.status}`).join(' · '):'No bookings'}</b></span></div><button class="button button-dark" type="submit">Save user changes</button></form>`;document.querySelector('#adminUserEdit').onsubmit=async event=>{event.preventDefault();const form=new FormData(event.currentTarget);await adminApi(`/api/admin/users/${id}`,{method:'PATCH',body:{name:form.get('name'),email:form.get('email'),phone:form.get('phone'),status:form.get('status')}});await loadAdminDirectories();showToast('User record updated');dialog.close()}}catch(error){content.innerHTML=`<p>${esc(error.message)}</p>`}}
document.querySelector('.platform-content').addEventListener('click',event=>{const row=event.target.closest('[data-record-type="guide"],[data-record-type="user"]');if(!row)return;event.preventDefault();event.stopImmediatePropagation();if(row.dataset.recordType==='guide')openGuide(row.dataset.recordId);else openUser(row.dataset.recordId)},true)})();
