const card=document.querySelector('#inviteCard'),token=new URLSearchParams(location.search).get('token');
const safe=value=>String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
function error(message){card.innerHTML=`<div class="error"><span class="eyebrow">INVITATION UNAVAILABLE</span><h1>This link cannot be used</h1><p class="intro">${safe(message)}</p><a class="button" href="index.html">Explore guides</a></div>`}
async function load(){if(!token)return error('The invitation token is missing. Ask the sender for a new link.');try{const response=await fetch(`/api/invitations/${encodeURIComponent(token)}`),data=await response.json();if(!response.ok)throw new Error(data.error||'Invitation not found');const invite=data.invitation;if(invite.status==='expired'||invite.status==='revoked')return error('This invitation has expired. Ask the sender to create a fresh link.');if(invite.kind==='client_booking')renderClient(invite);else renderGuide(invite)}catch(e){error(e.message)}}
function renderClient(invite){sessionStorage.setItem('fmgInviteToken',token);sessionStorage.setItem('fmgBookingDraft',String(invite.guideId));card.innerHTML=`<span class="eyebrow">PERSONAL BOOKING INVITATION</span><div class="guide"><i class="avatar" style="background-image:url('${safe(invite.guidePhoto)}')"></i><div><p>You have been invited by</p><h1>${safe(invite.guideName)}</h1><p>${safe(invite.guideLocation)}</p></div></div>${invite.message?`<div class="message">“${safe(invite.message)}”</div>`:''}<p class="intro">See only the dates ${safe(invite.guideName)} has made available. Your request is confirmed only after the guide accepts it; payment is arranged directly with the guide.</p><div class="steps"><span><b>1</b>Sign in or create a traveler account</span><span><b>2</b>Choose from open dates</span><span><b>3</b>Wait for guide confirmation</span></div><button class="button" id="continueBooking">View available dates ↗</button><a class="button secondary" href="index.html">Not now</a><small class="expiry">Invitation valid until ${new Date(`${invite.expiresAt}Z`).toLocaleDateString()}</small>`;document.querySelector('#continueBooking').onclick=async()=>{try{const session=await (await fetch('/api/auth/session',{credentials:'same-origin'})).json();if(session.user?.role==='traveler'){await fetch(`/api/invitations/${encodeURIComponent(token)}/accept`,{method:'POST',credentials:'same-origin'});location.href=`index.html?book=${invite.guideId}`}else location.href='traveler-login.html?next=book'}catch{location.href='traveler-login.html?next=book'}}}
function renderGuide(invite){sessionStorage.setItem('fmgGuideInviteToken',token);sessionStorage.setItem('fmgGuideInviteData',JSON.stringify({name:invite.name,email:invite.email,phone:invite.phone}));if(invite.applicationStatus==='approved'){card.innerHTML=`<span class="eyebrow">APPLICATION APPROVED</span><div class="guide"><div><p>Welcome to Chirps & Roar</p><h1>${safe(invite.name)}</h1></div></div><p class="intro">Your guide profile is approved. Create your password to activate the guide panel, open available dates, invite clients and respond to booking requests.</p><form id="activationForm"><label>Create password<input name="password" type="password" minlength="8" required placeholder="At least 8 characters"></label><button class="button" type="submit">Activate guide panel ↗</button></form>`;document.querySelector('#activationForm').onsubmit=async e=>{e.preventDefault();try{const response=await fetch(`/api/guide-onboarding/${encodeURIComponent(token)}/activate`,{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:e.currentTarget.password.value})}),data=await response.json();if(!response.ok)throw new Error(data.error||'Activation failed');sessionStorage.removeItem('fmgGuideInviteToken');sessionStorage.removeItem('fmgGuideInviteData');location.href=data.redirect}catch(err){error(err.message)}};return}if(invite.applicationStatus){card.innerHTML=`<span class="eyebrow">APPLICATION ${safe(invite.applicationStatus).toUpperCase()}</span><div class="guide"><div><p>Guide onboarding for</p><h1>${safe(invite.name)}</h1></div></div><p class="intro">Your application has been submitted and is currently ${safe(invite.applicationStatus.replace('_',' '))}. Return to this link after approval to activate your guide panel.</p><a class="button" href="index.html">Return home</a>`;return}card.innerHTML=`<span class="eyebrow">VERIFIED GUIDE ONBOARDING</span><div class="guide"><div><p>Chirps & Roar invitation for</p><h1>${safe(invite.name)}</h1></div></div>${invite.message?`<div class="message">“${safe(invite.message)}”</div>`:''}<p class="intro">Create your guide application, add the locations and specialties you work in, and submit it for platform review. Once approved, you can open dates, invite clients, and confirm booking requests.</p><div class="steps"><span><b>1</b>Complete your guide profile</span><span><b>2</b>Platform admin verifies it</span><span><b>3</b>Open dates and invite clients</span></div><a class="button" href="index.html?join=guide">Start guide application ↗</a><small class="expiry">Invitation valid until ${new Date(`${invite.expiresAt}Z`).toLocaleDateString()}</small>`}
load();
// Chirps & Roar frontend module.
